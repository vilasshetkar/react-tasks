pipeline {
    agent {
        docker {
            label 'docker'
            image 'nodeReact:0.1-nodejs'
        }
    }
	    stages {
        stage('Build') {
            steps {
                echo 'Build activity done'
            }
        }
		}
	
}
